# Tasks: 对齐 Clash Verge 接管后的完整 YAML

- [x] 记录无 Git 元数据基线并创建回滚副本；
- [x] 核对 Clash Verge v2.5.2 合并顺序和 Mihomo DNS/TUN/IPv6 字段；
- [ ] 调整完整 YAML 的双栈 DNS/TUN/IPv6 基线；
- [ ] 让域名与 IPv6 节点使用 Mihomo 原生解析，并把 bootstrap 降为可选覆盖；
- [ ] 更新 IR 与 YAML 回读校验；
- [ ] 把简单/完全模式文案收敛为规则复杂度；
- [ ] 更新前端草案、预览和运行态证据边界文案；
- [ ] 更新 golden YAML 与 Rust/TypeScript 测试；
- [ ] 同步 profile、desktop、security、validation Specs 与 spec-index；
- [ ] 运行 Spec、前端、Rust、构建和固定 Mihomo fixture 验证；
- [ ] 完成后将 change 标记为 `archived`。
