# Tasks: 为域名节点接入用户确认的受保护 bootstrap 映射

- [x] 记录当前目录没有 Git 元数据；
- [x] 定义 `bootstrapMappings[]` IPC contract 与秘密生命周期；
- [x] 在严格 Node IR 中校验和消费映射；
- [x] 在 YAML serializer 中输出顶层 `hosts`；
- [x] 在 IR 与 YAML 回读中验证域名节点闭环；
- [x] 在来源 UI 中增加映射编辑器、格式解析与脱敏提示；
- [x] 增加 Rust/TypeScript/golden 测试；
- [x] 同步 `profile-compilation`、`security-privacy`、`desktop-experience` 和 `spec-index.json`；
- [x] 运行 `npm run check:spec`、相关测试、构建与固定 Mihomo fixture 验证。
