# Tasks: 采用务实 DDD 与六边形架构

- [x] 记录现有 Git 基线不可用事实并保存临时回滚副本；
- [x] 创建 Rust `domain/application/infrastructure/interface` 层；
- [x] 将 catalog 模型、校验和 embedded repository 分离；
- [x] 将 profile DTO、领域服务和 catalog port 分离；
- [x] 将 source parser、URL 值对象、探测用例和 reqwest gateway 分离；
- [x] 将 Tauri commands 移至 `interface/ipc`；
- [x] 把 React 拆为 `app/features/shared` 功能切片；
- [x] 增加架构依赖漂移检查；
- [x] 同步当前能力 Specs 和 spec index；
- [x] 运行 Spec、格式、测试和构建门禁；
- [x] 通过后把 delta 合并到当前 Spec 并将本 change 标记为 archived。
