# Tasks: 固定预览操作并同步步骤导航

- [x] 记录当前目录无 Git 元数据；
- [x] 增加页面与预览栏滚动位置到 active 步骤的映射；
- [x] 让左侧导航动态设置 active 与 `aria-current`；
- [x] 把生成操作放入预览栏固定底部区域；
- [x] 增加步骤判定单元测试；
- [x] 同步 `desktop-experience` Spec 与 `spec-index.json`；
- [x] 运行 Spec、测试、构建和视觉交互验证；
- [x] 完成后将 change 标记为 `archived`。
