# Tasks: 将 VLESS 转换器拆为独立功能入口

- [x] 记录无 Git 元数据基线并建立回滚副本；
- [x] 记录内联入口与用户要求的差距；
- [x] 新建 `features/node-converter` 独立功能切片；
- [x] 在 App 顶栏增加一级功能切换；
- [x] 从 source-inspection 和 workspace controller 移除转换状态与 UI；
- [x] 增加独立输入模型测试和交互验证；
- [x] 同步 project、desktop/source/security Specs 与 spec-index；
- [x] 运行 Spec、前端、Rust 和构建门禁；
- [x] 完成后将 change 标记为 `archived`。
