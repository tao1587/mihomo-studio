# Tasks: 在节点输入区增加 VLESS 转换预览

- [x] 记录 Git 元数据缺失并建立回滚副本；
- [x] 定义独立转换请求/结果契约与秘密生命周期；
- [x] 从现有权威 VLESS 转换器输出 `proxies:` YAML；
- [x] 增加独立 IPC 与前端 API adapter；
- [x] 在直接节点编辑器内增加次级转换操作和敏感结果披露；
- [x] 增加单条、多条、Base64、去重、重名、兼容归一化和秘密不回显测试；
- [x] 同步 `source-ingestion`、`desktop-experience`、`security-privacy`、`system-architecture` 和 `spec-index.json`；
- [x] 运行 Spec、前端、Rust 与构建验证；
- [x] 完成后将本 change 标记为 `archived`。
